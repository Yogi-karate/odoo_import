language: python 
python: 
- '3.7'
jobs:
  include:
  - name: upload
  install:
  - pip install -r ./upload/requirements.txt
  script:
  - echo "Hello before Zappa"
  - zappa update dev -s ./upload/flask_gateway.json
  - name: import
  install:
  - pip install -r ./odoo/import_requirements.txt
  script:
  - echo "Hello before Zappa import"
  - zappa update dev -s ./odoo/import.json

notifications:
  slack: saboogroupworkspace:tco87KFtlVfRyVKhQoaEfKQn#smartdms-dev